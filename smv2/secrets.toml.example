# Copy this file to data/secrets.toml (NOT here at the repo root) and fill
# in the values you need. data/ is already gitignored; data/secrets.toml
# additionally has its own explicit .gitignore entry as defense in depth.
#
# Both keys are optional and only ever used as a fallback when the
# equivalent environment variable isn't set (env always wins) -- see
# app/config.py::anthropic_api_key/ollama_base_url.

# anthropic_api_key = "sk-ant-..."
# ollama_base_url = "http://localhost:11434"
